# ProspectPro Agent Credentials Template
# -----------------------------------------------------------------------------
# Copy to dev-tools/agents/.env.agent.local and hydrate via provider CLIs
# (Vercel, Supabase, GitHub). Keep this template checked in; never commit real
# secrets. Aligns with the consolidated `.env.local` structure.

# Shared automation tokens
AGENT_VERCEL_TEAM_SLUG=appsmithery
AGENT_SUPABASE_ACCESS_TOKEN=supabase_cli_token_placeholder
AGENT_SUPABASE_DEFAULT_JWT=jwt_placeholder
GITHUB_TOKEN=ghp_placeholder

# Supabase endpoints per environment
AGENT_DEV_SUPABASE_URL=https://your-project-ref.supabase.co
AGENT_DEV_SUPABASE_ANON_KEY=sb_publishable_placeholder
AGENT_STAGING_SUPABASE_URL=${AGENT_DEV_SUPABASE_URL}
AGENT_PROD_SUPABASE_URL=${AGENT_DEV_SUPABASE_URL}

# Highlight observability context (mirrors .env.example)
HIGHLIGHT_PROJECT_ID=highlight_project_placeholder
HIGHLIGHT_SERVICE_NAME=prospect-pro-agents

# Vercel deployment automation
VERCEL_TOKEN=vercel_token_placeholder

# Optional: external API keys routed through integration workflows
AGENT_GOOGLE_PLACES_API_KEY=google_places_placeholder
AGENT_HUNTER_IO_API_KEY=hunter_placeholder
AGENT_NEVERBOUNCE_API_KEY=neverbounce_placeholder
AGENT_FOURSQUARE_API_KEY=foursquare_placeholder
