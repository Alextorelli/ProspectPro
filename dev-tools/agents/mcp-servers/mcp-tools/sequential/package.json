{
  "name": "@prospectpro/mcp-sequential",
  "version": "0.2.0",
  "private": true,
  "description": "ProspectPro sequential thinking MCP server with session-aware logging",
  "license": "MIT",
  "author": "ProspectPro Engineering",
  "type": "module",
  "bin": {
    "prospectpro-mcp-sequential": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && shx chmod +x dist/*.js",
    "clean": "shx rm -rf dist",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.1",
    "chalk": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
