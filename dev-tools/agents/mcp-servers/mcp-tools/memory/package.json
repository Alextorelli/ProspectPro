{
  "name": "@prospectpro/mcp-memory",
  "version": "0.7.0",
  "private": true,
  "description": "ProspectPro knowledge graph memory MCP server with session-store defaults",
  "license": "MIT",
  "author": "ProspectPro Engineering",
  "type": "module",
  "bin": {
    "prospectpro-mcp-memory": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && shx chmod +x dist/*.js",
    "clean": "shx rm -rf dist",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.20.1"
  },
  "devDependencies": {
    "@types/node": "^22",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
